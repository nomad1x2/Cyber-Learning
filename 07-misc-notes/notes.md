# Git

- git add . (to include any new items in the commit)
- git status (to show the status of the current branch)
- git commit -m "message" (to commit with message)
- git push origin main (to push the commit to main branch)